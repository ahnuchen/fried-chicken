/**
 * 管理员
 */
class Admin {
	constructor(admin_id, admin_account, admin_password) {
		this.admin_id = admin_id
		this.admin_account = admin_account
		this.admin_password = admin_password
	}
}

module.exports = Admin
